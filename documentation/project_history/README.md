This directory holds preserved project-level planning and assessment documents that are useful for historical context but are not the live status tracker for the active workspace.

Use the repository root `README.md` and `ROADMAP.md` for the current state.
