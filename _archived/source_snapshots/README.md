These files are preserved source-tree snapshots, one-off patch artifacts, and retired scratch subcrates that were removed from active crate paths during `Operation Clean Slate`.

Nothing in this directory is part of the active workspace build.
