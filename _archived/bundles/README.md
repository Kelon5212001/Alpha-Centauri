This directory contains compressed archive bundles for preserved experiment trees that are intentionally no longer kept expanded in the repository.

Bundled here during `Operation Clean Slate`:

- `ai_development_framework.tar.gz`
- `data_processing.tar.gz`
- `deployment.tar.gz`
- `examples.tar.gz`
- `networking.tar.gz`
- `rendering_engine.tar.gz`
- `testing_framework.tar.gz`

These bundles are historical material only and are not part of the active build, test, validation, or runtime paths.
